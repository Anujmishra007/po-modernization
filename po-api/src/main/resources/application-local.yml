spring:
  application:
    name: po-modernization-local

  datasource:
    url: jdbc:h2:mem:podb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false

  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

server:
  port: 8080

# Temporal Configuration - Disabled for local development
temporal:
  enabled: false
  server:
    host: localhost
    port: 7233
  namespace: default

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

# Logging Configuration
logging:
  level:
    com.wms.po: DEBUG
    io.temporal: DEBUG
    org.springframework: INFO
    org.hibernate.SQL: DEBUG
